---
title: color_terms_zh-CN
date: 2025-10-19 10:51:05
author: DDLG
categories: DynamicColor
tags: DynamicColor
---
# 颜色术语

### 动态颜色

动态颜色代表一个 Material 颜色角色，其值由动态方案中的约束条件决定。

### Material 颜色角色

[Material 颜色角色](https://m3.material.io/styles/color/roles) 就像“数字填色”画布上的“数字”。它们是 UI 元素与颜色应用位置之间的连接组织。粗略地说，MCU 为每个颜色角色分配一个来自特定色调调色板的值。例如，`onSurface` 颜色在亮色方案中使用来自中性调色板的色调为 30 的颜色。

### 动态方案

动态方案由颜色属性组成，这些属性以预定的方式组合，以满足用户上下文或偏好的需求。它本质上是将颜色角色映射到色调调色板中特定色调的颜色。例如，primary = *207H 80C 90T*，onPrimary = *207H 80C 40T*。

### 方案变体

每个变体都是一组设计决策，涉及将色调调色板中的颜色值分配给颜色角色。

### 色调调色板

一组在 HCT 颜色空间中共享色相和色度，但在色调上有所不同的颜色。从感知的角度来看，我们可以说它们是“同一颜色的不同色调”。

MCU 生成 6 个色调调色板：主色、辅色、三次色、中性色、中性变体色和错误色。每个调色板包含从 0 到 100 的色调，作为将特定色调映射到特定角色的基础。

### 源颜色

提取出来用于定义所有五个关键颜色的单一颜色称为源颜色。虽然在代码中可能不需要它，但这是一个有用的区别，可以理解动态配色方案源于一个初始颜色，即色相、色度和色调。

### 关键颜色

关键颜色是理解动态颜色的一个有用概念。该术语描述了任何经过源颜色色相和色度转换的颜色。它不是提取的颜色，而是源颜色的派生。

关键颜色是代表色调调色板色相和色度的颜色。动态颜色使用 5 个关键颜色生成 5 个色调调色板，作为动态方案生成的基础。

### HCT

HCT 是色相、色度、色调的缩写。它是一个新的颜色空间的名称，该颜色空间使动态颜色成为可能。HCT 基于 [CAM16](https://onlinelibrary.wiley.com/doi/10.1002/col.22131) 的色相和色度；来自 L\*a\*b\* (CIELAB, 1976) 的亮度 L\* 构造表示为色调。

### 色相

例如红色、橙色、黄色、绿色等。色相范围可以从 0 到 359 度。

### 色度

通俗地说，色度描述了色彩的鲜艳程度。它类似于 HSL 颜色空间中的饱和度，但请注意两个区别：HCT 中的色度在感知上是准确的；对于色相和色调的每种组合，都有一个不同的最大色度。

### 色调

通俗地说，色调表示亮度的程度。HCT 颜色空间中的 T 等于 L\*a\*b\* 颜色空间中的 L\*。

### 对比度

颜色之间的差异。对于可访问性，对比度严格指色调的差异。色调相差 40 可保证 WCAG 对比度 ≥ 3.0；色调相差 50 可保证对比度 ≥ 4.5。

## 更多信息

有关更多 Material 术语，请参阅 https://m3.material.io/foundations/glossary。

## 引用
- [Dynamic Color](https://github.com/material-foundation/material-color-utilities)
